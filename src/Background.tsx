const Background: React.FC = () => {
  return (
    <div className="absolute flex min-h-screen w-full">
      <div className="w-full bg-[radial-gradient(ellipse_at_top_right,_var(--tw-gradient-stops))] from-fourth via-second to-fourth"></div>
    </div>
  );
};

export default Background;
