const Background: React.FC = () => {
  return (
    <div className="absolute flex min-h-screen w-full">
      <div className="w-full bg-[radial-gradient(ellipse_at_top_right,_var(--tw-gradient-stops))] from-emerald-500 via-sky-600 to-indigo-600"></div>
    </div>
  );
};

export default Background;
